{
    "$id": "docs/spec/transactions/payload.json",
    "title": "Metadata object",
    "description": "Metadata that applied to all other objects in the request",
    "type": "object",
    "properties": {
        "service": {
            "$ref": "../service.json"
        },
        "process": {
            "$ref": "../process.json"
        },
        "system": {
            "$ref": "../system.json"
        }
    },
    "required": ["service"]
}
